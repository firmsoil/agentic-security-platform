<!-- WEEK-2 STUB — see prompts/README.md. -->

# Verify a previously extracted node

Placeholder for the second-pass verification prompt. Will accept a
`(node, code_span)` pair and return `{verified: bool, reason: str}`.
This is the load-bearing call for the trust story — see ADR-0005 and
`memory/grounding_contract.md` for the contract.
