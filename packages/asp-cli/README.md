# asp-cli

Developer and CI CLI for the Agentic Security Platform.

## v0.1 commands

- `asp version` — print version
- `asp ontology show` — print the loaded ontology as JSON
- `asp ontology validate` — validate the bundled ontology YAML

More commands land as the platform matures: `asp scan`, `asp policy test`, `asp compliance evidence`.
